import api from "@/utils/api";

// Placeholder for user management APIs if they exist in the future
export const userService = {
  // Example:
  // getUserProfile: async () => {
  //   const response = await api.get('/users/me');
  //   return response.data;
  // }
};
